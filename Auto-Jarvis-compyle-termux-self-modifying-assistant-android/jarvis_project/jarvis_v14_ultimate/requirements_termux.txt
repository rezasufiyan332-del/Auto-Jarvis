# JARVIS v14 Ultimate - Termux Compatible Requirements
# Optimized for Android/Termux environment
# Total Size: ~500MB (vs 3-4GB original)

# ============================================================
# CORE DEPENDENCIES (Essential)
# ============================================================

# Core Python utilities
click>=8.0.0
psutil>=5.9.0
typing-extensions>=4.0.0

# Async and concurrency (lightweight)
aiofiles>=23.0.0
aiohttp>=3.8.0
asyncio>=3.4.3

# Data processing (lightweight versions)
numpy>=1.21.0,<1.24.0  # Termux compatible version
pandas>=1.5.0,<2.0.0   # Lighter version

# Web and API frameworks (minimal)
fastapi>=0.95.0
uvicorn[standard]>=0.20.0
pydantic>=1.10.0
httpx>=0.24.0
requests>=2.28.0

# Database (SQLite only, no external servers)
sqlalchemy>=1.4.0
alembic>=1.9.0

# ============================================================
# AI/ML ALTERNATIVES (Lightweight)
# ============================================================

# Instead of TensorFlow/PyTorch - use lightweight alternatives:
# scikit-learn for basic ML (NO heavy deep learning)
scikit-learn>=1.1.0,<1.3.0

# OpenAI API (cloud-based, no local models)
openai>=0.27.0

# Hugging Face Inference API (cloud-based)
huggingface-hub>=0.15.0

# ============================================================
# NLP (Lightweight)
# ============================================================

# Basic NLP without transformers
nltk>=3.8
textblob>=0.17.1

# Regex and text processing
regex>=2023.6.0
chardet>=5.1.0

# ============================================================
# IMAGE PROCESSING (Lightweight)
# ============================================================

# Pillow instead of OpenCV (much lighter)
Pillow>=9.5.0

# ============================================================
# AUDIO (Termux Compatible)
# ============================================================

# SpeechRecognition (Google Speech API - cloud-based)
SpeechRecognition>=3.10.0

# NO pyaudio, NO pyttsx3 (use Termux-API instead)

# ============================================================
# DATA VISUALIZATION (Lightweight)
# ============================================================

matplotlib>=3.6.0,<3.8.0  # Lighter version
seaborn>=0.12.0

# NO plotly, NO bokeh (too heavy)

# ============================================================
# WEB FRAMEWORKS (Minimal)
# ============================================================

flask>=2.2.0  # Lightweight web framework

# NO Django, NO Streamlit, NO JupyterLab

# ============================================================
# CODE QUALITY (Essential only)
# ============================================================

pytest>=7.3.0
coverage>=7.2.0

# ============================================================
# SECURITY
# ============================================================

cryptography>=40.0.0
pyjwt>=2.6.0
bcrypt>=4.0.0
passlib>=1.7.0

# ============================================================
# FILE PROCESSING
# ============================================================

python-docx>=0.8.11
openpyxl>=3.1.0
reportlab>=4.0.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# ============================================================
# CONFIGURATION
# ============================================================

python-dotenv>=1.0.0
pyyaml>=6.0
configobj>=5.0.0

# ============================================================
# LOGGING AND MONITORING (Lightweight)
# ============================================================

loguru>=0.7.0
colorlog>=6.7.0

# NO sentry-sdk, NO prometheus (too heavy)

# ============================================================
# NETWORK
# ============================================================

websockets>=11.0.0
paramiko>=3.1.0

# ============================================================
# UTILITIES
# ============================================================

schedule>=1.2.0
colorama>=0.4.6
tqdm>=4.65.0
rich>=13.3.0
watchdog>=3.0.0

# ============================================================
# DATA SERIALIZATION
# ============================================================

orjson>=3.8.0
msgpack>=1.0.0

# ============================================================
# DATE AND TIME
# ============================================================

python-dateutil>=2.8.0
pytz>=2023.3
arrow>=1.2.0

# ============================================================
# TERMUX-SPECIFIC PACKAGES
# ============================================================

# Termux API wrapper (for native Android features)
# Install via: pkg install termux-api
# Then: pip install termux-api-wrapper
# Note: Uncomment below after installing termux-api package
# termux-api-wrapper>=0.1.0

# ============================================================
# BUILD TOOLS (Minimal)
# ============================================================

setuptools>=65.0.0
wheel>=0.40.0

# ============================================================
# NOTES FOR TERMUX INSTALLATION
# ============================================================

# 1. First install system packages:
#    pkg update && pkg upgrade
#    pkg install python python-pip
#    pkg install libxml2 libxslt libjpeg-turbo
#    pkg install termux-api
#
# 2. Install Python packages:
#    pip install --upgrade pip
#    pip install -r requirements_termux.txt
#
# 3. For audio features (optional):
#    pkg install sox
#    # Use Termux-API for TTS: termux-tts-speak "text"
#
# 4. Storage access:
#    termux-setup-storage
#
# 5. Memory optimization:
#    Set JARVIS_MAX_MEMORY=100 in config

# ============================================================
# REMOVED PACKAGES (Too heavy/incompatible for Termux)
# ============================================================

# ❌ tensorflow>=2.11.0          (2GB+, no ARM mobile build)
# ❌ torch>=1.13.0               (1GB+, limited ARM support)
# ❌ transformers>=4.21.0        (500MB+, depends on torch)
# ❌ sentence-transformers       (depends on torch)
# ❌ opencv-python               (requires complex compilation)
# ❌ opencv-contrib-python       (requires complex compilation)
# ❌ pyaudio                     (needs PortAudio, use Termux-API)
# ❌ pyttsx3                     (needs system TTS, use Termux-API)
# ❌ django>=4.2.0               (too heavy for mobile)
# ❌ jupyterlab                  (too heavy)
# ❌ streamlit                   (too heavy)
# ❌ memcached                   (server not available)
# ❌ redis                       (use SQLite instead)
# ❌ aioredis                    (depends on redis)
# ❌ numba                       (compilation issues)
# ❌ cython                      (compilation issues)
# ❌ scipy>=1.9.0                (too heavy, optional)
# ❌ librosa                     (depends on scipy)
# ❌ spacy>=3.5.0                (too heavy, use nltk)
# ❌ plotly>=5.14.0              (too heavy)
# ❌ bokeh>=3.1.0                (too heavy)

# ============================================================
# LIGHTWEIGHT ALTERNATIVES
# ============================================================

# Instead of TensorFlow/PyTorch → Use cloud APIs:
#   - OpenAI API (openai package)
#   - Hugging Face Inference API (huggingface-hub)
#   - Google Cloud AI APIs
#
# Instead of OpenCV → Use Pillow
# Instead of PyAudio → Use Termux-API (termux-microphone-record)
# Instead of pyttsx3 → Use Termux-API (termux-tts-speak)
# Instead of Redis → Use SQLite
# Instead of Spacy → Use NLTK
# Instead of heavy visualization → Use Matplotlib only
